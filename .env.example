# Local secrets / API keys — shape-only template, tracked in git.
# Copy this to .env (gitignored) and fill in real values there.
# No variables defined yet — add one here (name only, no value) whenever
# the app needs its first secret, and mention it in your PR description.
